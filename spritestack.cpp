#include "engine.h"

/*** CREATED BY GEIGER ALEXIS ***
 *** devcomm 1 : the number of slice, the width and the height of the slice ***
 *** must be declared manually for now ***/

void Engine::spritestack()
{
	m_Texture.loadFromFile("Graphics/Hunter.png");
	int sliceNumber = 16;
	int sliceWidth = 16;
	int sliceHeight = 16;

/* TO DO : 
 * For loop to get the real sprite stacking : having first a sprite already declared ? 
 * what should be returned ? Nothing ? To test.
 * should the texture be in the parameters instead ? */  
}
